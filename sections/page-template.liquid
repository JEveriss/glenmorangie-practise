{{ 'page.css' | asset_url | stylesheet_tag }}

<div class="page-wrapper">

  <h1 class="page-title">{{ page.title }}</h1>
  <div class="page-paragraph">
    {% #   {% if request.path == '/pages/environment' %}
    {% #    {% render 'dropdown-menu' %}
    {% #  {% endif %}
    {{ page.content }}

  </div>
</div>