<html>
  <head>
    {{ content_for_header }}
    {{ 'style.css' | asset_url | stylesheet_tag }}
  </head>
  <body>
    {% render 'header' %}
    <h1>{{ shop.name }}</h1>
    {{ content_for_layout }}
  </body>
</html>